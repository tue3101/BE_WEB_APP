# TODO: Fix StackOverflowError in Authentication

## Tasks
- [x] Add @EnableAspectJAutoProxy(proxyTargetClass = true) to SecurityConfig.java
- [ ] Test the application to ensure the error is resolved
